services:
  - type: web
    name: afrochatbot
    env: php
    plan: free
    buildCommand: "apt-get update && apt-get install -y curl && curl -sS https://getcomposer.org/installer | php && mv composer.phar /usr/local/bin/composer && composer install"
    startCommand: "apache2-foreground"
